# Known Limitations

There are no open known limitations against the current documented local review workflow.

The implementation uses Pygments for syntax highlighting, supports mouse drag range selection when the terminal reports drag events, and supports editing and deleting saved comments through command mode.
